# Auto detect text files and perform LF normalization
* text=auto

# Media is binary. Never let git normalise line endings inside it.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.mp4  binary
*.mov  binary
*.pdf  binary
